[[filters]]
name = "aws"
match = "^aws "
strip_ansi = true
truncate_at = 200
max_lines = 50

[[filters]]
name = "psql"
match = "^psql"
strip_ansi = true
drop_lines = "^[\\-\\+]+$"
truncate_at = 200
max_lines = 50
